server:
  port: 9090
spring:
  application:
    name: Inventario-API
  datasource:
    url: jdbc:mysql://localhost:3306/api_inventario
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    